# ---- 数据库（可选）----
# 不配置时：数据保存在浏览器 localStorage + 函数内存，公开链接依然可完整演示。
# 配置后：写入托管 SQLite（libSQL / Turso），数据长期持久化。

# Turso（线上推荐）：https://turso.tech
# TURSO_DATABASE_URL="libsql://your-db-name.turso.io"
# TURSO_AUTH_TOKEN=""

# 本地单文件 SQLite（开发用，真正落地成一个 .db 文件）
# DATABASE_URL="file:./.data/roommate.db"
